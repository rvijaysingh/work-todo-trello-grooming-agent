{rules}

## Context spine (world state)
{spine}

## Board summary
{board_summary}

## Output contract
Respond with schema-valid JSON only — no prose, no markdown fences. Use only the
card ids given to you. Never invent people, projects, or entities that do not
appear in the source cards or the spine above. Every factual claim (ids, dates,
list names, counts) is supplied to you as a constraint; do not assert new facts.
